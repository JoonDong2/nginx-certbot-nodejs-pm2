{
  "apps": [{
    "name": "<앱 이름>",
    "script": "src/app.js", // 실행할 파일
    "instances": "max",
    "exec_mode": "cluster",
    "env": {
      "NODE_ENV": "development"
      // 사용할 환경변수 입력, 없으면 해당 주석 삭제
    },
    "env_production" : {
      "NODE_ENV": "production"
      // 사용할 환경변수 입력, 없으면 해당 주석 삭제
    }
  }]
}